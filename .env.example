# Database
DATABASE_URL="postgresql://privacy_pilot:privacy_pilot_dev@localhost:5432/privacy_pilot"

# NextAuth
NEXTAUTH_SECRET="change-me-to-a-random-secret-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Storage
STORAGE_TYPE="local"
STORAGE_LOCAL_PATH="./uploads"

# For S3 (production):
# STORAGE_TYPE="s3"
# S3_BUCKET="privacy-pilot-docs"
# S3_REGION="us-east-1"
# S3_ACCESS_KEY_ID=""
# S3_SECRET_ACCESS_KEY=""
# S3_ENDPOINT=""

# Integration secrets encryption (32 bytes, base64-encoded)
# Generate with: openssl rand -base64 32
INTEGRATION_ENCRYPTION_KEY=""

# AWS Integration mock mode (set to "true" for dev without real AWS credentials)
# When enabled, test/scan endpoints return deterministic fake data.
# AWS_INTEGRATION_MOCK=true

# SLA default (days)
DEFAULT_SLA_DAYS=30

# Module 8.6: Integration Hardening
# Encryption key for connector secrets + webhook secrets (32 bytes, base64-encoded)
# Generate with: openssl rand -base64 32
# Falls back to insecure dev default if not set (not for production!)
PRIVACYPILOT_SECRET=""
