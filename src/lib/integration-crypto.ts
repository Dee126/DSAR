/**
 * @deprecated Use `@/lib/security/encryption` directly.
 * This module is kept for backward compatibility only.
 */

export { encrypt as encryptIntegrationSecret, decrypt as decryptIntegrationSecret } from "@/lib/security/encryption";
